[package]
name = "action"
version = "0.1.0"
edition = "2024"

[dependencies]
checksum_core = { package = "core", path = "../core" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3.25.0"
